# libgoc Benchmarks

These benchmarks are built separately from the main libgoc library build.

Run all benchmarks with a single command:

```sh
make run
```

Prerequisites: a C11 compiler, CMake, pkg-config, libuv, and a threaded Boehm GC (bdw-gc).
